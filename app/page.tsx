"use client";
import Link from "next/link";import {ArrowRight,Sparkles} from "lucide-react";import {useProject} from "@/components/project-provider";import {Button,Card,PageHeading} from "@/components/ui";
function Stat({label,value,hint}:{label:string;value:number|string;hint:string}){return <Card className="p-5"><div className="text-xs uppercase tracking-wider text-zinc-500">{label}</div><div className="mt-2 text-3xl font-black">{value}</div><div className="mt-1 text-xs text-zinc-500">{hint}</div></Card>}
export default function Page(){const{project}=useProject();const generated=project.scenes.filter(s=>s.generationStatus==="completed").length;return <><PageHeading eyebrow="Workspace" title="Turn one chapter into a visual sequence." description="Analyze long-form prose, build reusable character/location bibles, and turn visual beats into consistent 16:9 scene assets." action={<Link href="/story"><Button><Sparkles size={16}/> Start with a story</Button></Link>}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat label="Characters" value={project.characters.length} hint="Reusable cast references"/><Stat label="Locations" value={project.locations.length} hint="Environment continuity"/><Stat label="Scenes" value={project.scenes.length} hint="Automatic visual beats"/><Stat label="Generated" value={generated} hint="Image assets"/></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><Card className="p-7"><div className="mb-3 inline-flex rounded-full border border-violet-400/20 bg-violet-400/10 px-3 py-1 text-xs text-violet-300">YouTube Novel Explained preset</div><h2 className="text-2xl font-black">Story → cast → locations → scenes → storyboard</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-zinc-400">The current MVP works without API keys using a server-side mock analyzer and mock image provider. Real provider adapters can replace them later.</p><Link href="/story" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-violet-300">Analyze a chapter <ArrowRight size={15}/></Link></Card><Card className="p-6"><div className="text-sm font-bold">Pipeline status</div><div className="mt-5 space-y-4 text-sm">{[["Story analysis",project.scenes.length>0],["Character bible",project.characters.length>0],["Location bible",project.locations.length>0],["Storyboard",project.scenes.length>0]].map(([l,d])=><div key={String(l)} className="flex justify-between"><span className="text-zinc-400">{String(l)}</span><span className={d?"text-emerald-400":"text-zinc-600"}>{d?"Ready":"Waiting"}</span></div>)}</div></Card></div></>}
