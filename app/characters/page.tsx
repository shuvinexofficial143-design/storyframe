"use client";
import {Lock,LockOpen,UserRound} from "lucide-react";import {useProject} from "@/components/project-provider";import {Badge,Button,Card,PageHeading} from "@/components/ui";
export default function Characters(){const{project,updateCharacter}=useProject();return <><PageHeading eyebrow="Step 2" title="Character Bible" description="Recurring characters get stable IDs and reusable appearance rules. Lock approved designs before generation."/><div className="grid gap-4 lg:grid-cols-2 2xl:grid-cols-3">{project.characters.map(c=><Card key={c.id} className="overflow-hidden"><div className="grid h-40 place-items-center border-b border-white/8 bg-[radial-gradient(circle_at_center,rgba(139,92,246,.2),transparent_60%)]"><UserRound size={58} className="text-violet-300"/></div><div className="p-5"><div className="flex items-center justify-between"><div><div className="font-bold">{c.name}</div><div className="mt-1 text-xs text-zinc-500">{c.role}</div></div><Badge>{c.locked?"Locked":"Editable"}</Badge></div><label className="mt-4 block text-xs text-zinc-500">Appearance</label><textarea value={c.appearance} onChange={e=>updateCharacter(c.id,{appearance:e.target.value})} className="mt-2 min-h-20 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none"/><label className="mt-3 block text-xs text-zinc-500">Outfit</label><input value={c.outfit} onChange={e=>updateCharacter(c.id,{outfit:e.target.value})} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none"/><Button variant={c.locked?"secondary":"primary"} className="mt-4 w-full" onClick={()=>updateCharacter(c.id,{locked:!c.locked})}>{c.locked?<LockOpen size={15}/>:<Lock size={15}/>} {c.locked?"Unlock character":"Lock character"}</Button></div></Card>)}{project.characters.length===0&&<Card className="p-8 text-sm text-zinc-500">Analyze a story first. Extracted characters will appear here.</Card>}</div></>}
