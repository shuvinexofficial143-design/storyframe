"use client";
import {useProject} from "@/components/project-provider";import {Button,Card,PageHeading,Badge} from "@/components/ui";
export default function Projects(){const{project,updateProject,resetProject}=useProject();return <><PageHeading eyebrow="Project manager" title="Projects" description="The MVP stores the active project locally. Supabase schema is included for later cloud persistence." action={<Button onClick={resetProject}>New blank project</Button>}/><Card className="p-6"><div className="flex flex-col gap-5 md:flex-row md:items-center md:justify-between"><div><div className="flex items-center gap-2"><h2 className="font-bold">{project.name}</h2><Badge>Active</Badge></div><div className="mt-2 text-sm text-zinc-500">{project.scenes.length} scenes · {project.characters.length} characters · {project.locations.length} locations</div></div><input className="rounded-xl border border-white/10 bg-black/20 px-4 py-2 text-sm outline-none" value={project.name} onChange={e=>updateProject({name:e.target.value})}/></div></Card></>}
