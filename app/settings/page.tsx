"use client";
import {Download,ShieldCheck} from "lucide-react";import {useProject} from "@/components/project-provider";import {Button,Card,PageHeading} from "@/components/ui";
export default function Settings(){const{project}=useProject();const download=()=>{const blob=new Blob([JSON.stringify(project,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='storyframe-project.json';a.click();URL.revokeObjectURL(url)};return <><PageHeading eyebrow="Configuration" title="Settings & Export" description="Real provider keys belong on the server. The current MVP stays in mock mode until you connect a provider."/><div className="grid gap-5 lg:grid-cols-2"><Card className="p-6"><div className="flex items-center gap-2 font-bold"><ShieldCheck size={17}/> Provider architecture</div><p className="mt-3 text-sm leading-6 text-zinc-400">Use .env.local for IMAGE_PROVIDER, AI_TEXT_PROVIDER and API keys. Never expose secret keys through NEXT_PUBLIC variables.</p><div className="mt-5 rounded-xl border border-white/8 bg-black/20 p-4 font-mono text-xs leading-6 text-zinc-400">IMAGE_PROVIDER=mock<br/>AI_TEXT_PROVIDER=mock<br/>OPENAI_API_KEY=</div></Card><Card className="p-6"><div className="font-bold">Project export</div><p className="mt-3 text-sm leading-6 text-zinc-400">Export story, cast, locations, prompts, generated assets and timeline metadata.</p><Button className="mt-5" onClick={download}><Download size={16}/> Export project JSON</Button></Card><Card className="p-6 lg:col-span-2"><div className="text-sm font-bold">Content rights notice</div><p className="mt-2 text-sm text-zinc-500">Only upload or generate from stories you own, are licensed to use, or are otherwise legally permitted to transform.</p></Card></div></>}
