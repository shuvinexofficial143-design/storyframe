import {NextResponse} from "next/server";import {z} from "zod";
const Input=z.object({sceneNumber:z.number().int().positive(),prompt:z.string().min(1).max(12000)});
function esc(v:string){return v.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&apos;"}[c]||c))}
function wrap(t:string,m=58){const words=t.split(/\s+/);const lines:string[]=[];let line="";for(const word of words){if((line+" "+word).trim().length>m){lines.push(line);line=word}else line=(line+" "+word).trim()}if(line)lines.push(line);return lines.slice(0,5)}
export async function POST(req:Request){const p=Input.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid request"},{status:400});const{sceneNumber,prompt}=p.data;const hue=(sceneNumber*47)%360;const tspans=wrap(prompt).map((l,i)=>`<tspan x="70" dy="${i?30:0}">${esc(l)}</tspan>`).join("");const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="1280" height="720"><defs><linearGradient id="g" x2="1" y2="1"><stop stop-color="hsl(${hue} 55% 22%)"/><stop offset="1" stop-color="#08090c"/></linearGradient></defs><rect width="1280" height="720" fill="url(#g)"/><circle cx="1020" cy="130" r="360" fill="hsl(${(hue+60)%360} 80% 60%)" opacity=".12"/><path d="M0 570 C250 430 420 650 650 530 S1000 430 1280 560 V720 H0Z" fill="#050608" opacity=".75"/><text x="70" y="90" fill="white" font-size="24" font-family="Arial" font-weight="700">STORYFRAME · SCENE ${String(sceneNumber).padStart(3,"0")}</text><text x="70" y="430" fill="white" font-size="24" font-family="Arial" opacity=".9">${tspans}</text><text x="70" y="650" fill="white" opacity=".5" font-size="18" font-family="Arial">Mock Image Provider · ready for real provider adapter</text></svg>`;return NextResponse.json({image:`data:image/svg+xml;charset=utf-8,${encodeURIComponent(svg)}`,provider:"mock"})}
