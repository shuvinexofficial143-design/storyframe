"use client";
import {Lock,LockOpen,MapPinned} from "lucide-react";import {useProject} from "@/components/project-provider";import {Badge,Button,Card,PageHeading} from "@/components/ui";
export default function Locations(){const{project,updateLocation}=useProject();return <><PageHeading eyebrow="Step 3" title="Location Bible" description="Preserve architecture, lighting, landmarks, furniture and important prop positions across recurring environments."/><div className="grid gap-4 lg:grid-cols-2">{project.locations.map(l=><Card key={l.id} className="p-5"><div className="flex items-start justify-between"><div className="flex gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-400/10 text-cyan-300"><MapPinned size={18}/></div><div><div className="font-bold">{l.name}</div><div className="text-xs text-zinc-500">Environment reference</div></div></div><Badge>{l.locked?"Locked":"Editable"}</Badge></div><div className="mt-5 grid gap-3"><input value={l.architecture} onChange={e=>updateLocation(l.id,{architecture:e.target.value})} className="rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none"/><input value={l.lighting} onChange={e=>updateLocation(l.id,{lighting:e.target.value})} className="rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none"/><textarea value={l.continuity} onChange={e=>updateLocation(l.id,{continuity:e.target.value})} className="min-h-24 rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none"/></div><Button variant={l.locked?"secondary":"primary"} className="mt-4" onClick={()=>updateLocation(l.id,{locked:!l.locked})}>{l.locked?<LockOpen size={15}/>:<Lock size={15}/>} {l.locked?"Unlock":"Lock location"}</Button></Card>)}{project.locations.length===0&&<Card className="p-8 text-sm text-zinc-500">Analyze a story first. Extracted locations will appear here.</Card>}</div></>}
